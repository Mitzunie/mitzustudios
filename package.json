{
  "name": "mitzustudios",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd apps/web && npm run dev",
    "build": "cd apps/web && npm run build",
    "lint": "cd apps/web && npm run lint",
    "test": "cd apps/web && npm run test",
    "format": "cd apps/web && npm run format",
    "format:check": "cd apps/web && npm run format:check",
    "db:generate": "cd apps/web && npm run db:generate",
    "db:migrate:dev": "cd apps/web && npm run db:migrate:dev",
    "db:migrate:deploy": "cd apps/web && npm run db:migrate:deploy",
    "db:studio": "cd apps/web && npm run db:studio"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
